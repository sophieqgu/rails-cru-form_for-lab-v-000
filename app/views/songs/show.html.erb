<h1>@song.name</h1>

<%= form_for @song do |f| %>
  <%= f.label :song_name %>
  <%= f.string :name %><br>

  <%= f.label :song_artist_id %>
  <%= f.integer link_to :artist_id, artist_path(Artist.find(artist_id)) %>

  <%= f.label :song_genre_id %>
  <%= f.integer link_to :genre_id. genre_path(Genre.find(genre_id)) %>

<% end %>
